import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const MSI = () => {
  return (
    <View>
      <Text>MSI</Text>
    </View>
  );
};

export default MSI;

const styles = StyleSheet.create({});
